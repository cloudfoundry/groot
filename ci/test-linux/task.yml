---
platform: linux

inputs:
- name: groot

params:
  DOCKER_REGISTRY_USERNAME:
  DOCKER_REGISTRY_PASSWORD:
  SKIP_PRIVATE_DOCKER_REGISTRY_TESTS:

caches:
- path: pkg

run:
  dir: groot
  path: ci/test-linux/task

image_resource:
  type: registry-image
  source:
    repository: cloudfoundry/tas-runtime-build
    tag: latest

